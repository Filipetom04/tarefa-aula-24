# Aula 24: Refactoring e Testes Automatizados

Este repositório contém o exercício prático da Aula 24, focado em transformar código legado em código profissional através de técnicas de Refactoring e Testes Unitários.

## 🛠️ Como Executar

1. **Instale as dependências:**
```bash
   npm install
   ```

2. **Rode os Testes:**
   Para verificar se a lógica está funcionando corretamente:

   ```bash
   npm test
   ```

3. **Verifique a Qualidade do Código (Lint):**
   Para buscar erros de padrão:

   ```bash
   npm run lint
   ```

4. **Formate o Código:**
   Para padronizar a indentação e estilo:
   ```bash
   npm run format
   ```

## 📝 O que foi feito (Checklist)

- [x] Seleção de módulo simulado (`post.js`).
- [x] Refatoração (Troca de `var` por `const`, nomes descritivos, sintaxe moderna).
- [x] Criação de testes unitários com Jest.
- [x] Configuração de ferramentas de qualidade (ESLint e Prettier).
